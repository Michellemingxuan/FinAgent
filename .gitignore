# Credentials — never commit
.env

# Note: output/.last_context.pkl IS committed so removal runs can re-render the
# page from cached data without a full report run. It holds the same market data
# already published in the HTML report.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
